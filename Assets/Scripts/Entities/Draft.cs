using UnityEngine;

public class Draft : MonoBehaviour
{
  private void OnTriggerEnter2D(Collider2D collider)
  {
    /*if (collider.tag == "Trash")
      TrashManager.Instance.MoveOnDraft(collider.gameObject);*/
  }
}
